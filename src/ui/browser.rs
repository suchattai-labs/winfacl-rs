//! Two-panel browser (task 11). Stub until the editor lands.

use std::path::Path;
use std::process::ExitCode;

pub fn run(_root: &Path, _follow: bool) -> ExitCode {
    eprintln!("winfacl: browser not implemented yet");
    ExitCode::FAILURE
}
